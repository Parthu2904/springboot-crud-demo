# 🚀 Spring Boot CRUD Demo

A minimal yet production-style REST API using **Spring Boot 3**, **JPA**, and **H2**.

### 🧰 Tech Stack
- Java 17  
- Spring Boot 3.3  
- Spring Data JPA  
- Lombok  
- H2 Database  
- Maven  

### 💡 Highlights
- RESTful CRUD API with validation  
- Layered architecture (Controller → Service → Repository)  
- YAML-based configuration  
- Uses H2 in-memory DB (auto-reset every run)

### 🧪 Run Locally
```bash
mvn spring-boot:run
